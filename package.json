{
  "name": "mini-rag-app-frontend",
  "version": "1.0.0",
  "description": "Frontend deployment configuration for Mini RAG App",
  "scripts": {
    "build": "node build-config.js"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
